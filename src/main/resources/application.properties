#server.port=9000
#spring.datasource.url=jdbc:postgresql://localhost:5432/tenpercent
#spring.datasource.username=postgres
#spring.datasource.password=password

# Enabling H2 Console
spring.h2.console.enabled=true
# Turn Statistics on
spring.jpa.properties.hibernate.generate_statistics=true
logging.level.org.hibernate.stat=debug
# Show all queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type=trace

spring.datasource.url=jdbc:h2:mem:testdb
spring.data.jpa.repositories.bootstrap-mode=default

# This will ensure that only script-based schema generation is performed using schema.sql.
spring.jpa.hibernate.ddl-auto=none
# Above script-based initialization is performed by default only for embedded databases.
# To always initialize a database using scripts, we'll have to use:
# spring.sql.init.mode=always
